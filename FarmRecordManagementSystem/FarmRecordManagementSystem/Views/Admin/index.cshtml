@{
    Layout = "_Layout";
}

<partial name="_Notification" />

<div class="card-container d-flex flex-wrap">
    <div class="card mt-4 mx-2" style="width: 25rem;">
        <div class="card-body">
            <h5 class="card-title fs-3">Total Farms</h5>
            <p class="card-text fs-3">22</p>
            @* <a href="@Url.Action("GetFarmDetails", "Farm", new { farmId = farm.Id })" class="btn btn-primary">View
            Details</a> *@
        </div>
    </div>
</div>

<div class="card-container d-flex flex-wrap">
    <div class="card mt-4 mx-2" style="width: 25rem;">
        <div class="card-body">
            <h5 class="card-title fs-3">Number Of Users</h5>
            <p class="card-text fs-3">22</p>
            <a href="@Url.Action("GetFarmDetails", "Farm", new { farmId = farm.Id })" class="btn btn-primary">View
                Details</a>
        </div>
    </div>
</div>