@model CropTypes
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@{
    Layout = "_LayoutAdmin";
}
<partial name="_Notification" />

<h2 class="mt-2">Add Crop Types</h2>

<form asp-action="AddCropTypes" method="post">
    <div class="form-group">
        @* <input type="hidden" asp-for="FarmId" value="@farmId" /> *@
        <div class="col-4">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" />
        </div>
        <div class="mt-2">
            <button type="submit" class="btn btn-primary mt-1 mb-2">Add</button>
            <a class="btn btn-danger mx-1 mb-2" onclick="window.history.back()">Back</a>
        </div>
    </div>
</form>
